{
  "name": "erp-frontend",
  "private": true,
  "version": "1.0.0",
  "workspaces": [
    "packages/constants",
    "packages/contracts"
  ],
  "scripts": {
    "build": "yarn workspaces run build",
    "clean": "rimraf node_modules packages\/**\/node_modules packages\/**\/dist"
  },
  "devDependencies": {
    "rimraf": "^6.0.1",
    "typescript": "^5.9.2"
  },
  "engines": {
    "node": ">=20.18.1",
    "yarn": ">=1.22.19"
  }
}
